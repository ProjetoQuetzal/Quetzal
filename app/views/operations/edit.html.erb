<h1>Alterando a operacao <% @operation.name %></h1>

<%= form_for(:operation, :url => {:action => 'update'}) do |f| %>

	Name: <%= f.text_field :name %><br/>

  <p>
  	<% @roles.each do |role| %>
  	<div>
  		<%= check_box_tag "role_titles[]", role.title, @operation.roles.include?(role) %>
  		<%= role.title %>
  	</div>
  	<% end %>
  </p>

  <%= f.submit "Save" %>

<% end %>
